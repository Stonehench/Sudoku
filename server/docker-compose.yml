name: sudoku-server

services:
  sudoku-server:
    build: ..
    container_name: sudoku-server
    network_mode: "host"
    ports:
      - "5000:5000"
    environment:
      - dbhost=localhost
      - SPRING_PROFILES_ACTIVE=prod